# Modo demo (login sin credenciales, datos de ejemplo). Omitir en producción.
VITE_DEMO_MODE=

# Neon Auth — Base URL (Consola Neon → Auth → Configuration)
VITE_NEON_AUTH_URL=

# Neon Data API — URL REST (Consola Neon → Data API)
VITE_NEON_DATA_API_URL=

# Supabase (solo para la edge function del asistente IA, opcional)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
